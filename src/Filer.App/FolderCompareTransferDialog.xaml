<Window x:Class="Filer.App.FolderCompareTransferDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{DynamicResource Surface.Background}" Foreground="{DynamicResource Fg.Primary}"
        Title="転送して閉じる">
    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource DialogButton}"/>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}"/>
            <Setter Property="Margin" Value="2,4"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{DynamicResource Fg.Secondary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}"/>
        </Style>
    </Window.Resources>
    <StackPanel Margin="14" Width="320">
        <TextBlock Foreground="{DynamicResource Fg.Subtle}" FontSize="11" TextWrapping="Wrap" Margin="2,0,2,8"
                   Text="チェックした項目を各ペインに一覧として転送し、比較を閉じます。"/>

        <GroupBox Header="左ペインへ転送">
            <StackPanel Margin="6,4">
                <CheckBox x:Name="LeftDiff" Content="差分"/>
                <CheckBox x:Name="LeftSame" Content="重複"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="右ペインへ転送" Margin="0,10,0,0">
            <StackPanel Margin="6,4">
                <CheckBox x:Name="RightDiff" Content="差分"/>
                <CheckBox x:Name="RightSame" Content="重複"/>
            </StackPanel>
        </GroupBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="転送(_O)" Width="90" IsDefault="True" Click="Ok_Click" Margin="0,0,8,0"/>
            <Button Content="キャンセル" Width="90" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
