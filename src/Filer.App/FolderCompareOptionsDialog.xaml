<Window x:Class="Filer.App.FolderCompareOptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{DynamicResource Surface.Background}" Foreground="{DynamicResource Fg.Primary}"
        Title="フォルダー比較">
    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource DialogButton}"/>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}"/>
            <Setter Property="Margin" Value="2,4"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{DynamicResource Fg.Secondary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}"/>
        </Style>
    </Window.Resources>
    <StackPanel Margin="14" Width="320">
        <TextBlock x:Name="TargetText" Foreground="{DynamicResource Fg.Subtle}" FontSize="11"
                   TextWrapping="Wrap" Margin="2,0,2,8"/>

        <GroupBox Header="変更の判定基準">
            <StackPanel Margin="6,4">
                <CheckBox x:Name="CompareSize" Content="サイズで比較(_S)"/>
                <CheckBox x:Name="CompareDate" Content="更新日時で比較(_D)"/>
                <CheckBox x:Name="CompareContent" Content="内容(バイト単位)で比較(_C)"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="表示" Margin="0,10,0,0">
            <StackPanel Margin="6,4">
                <CheckBox x:Name="Recursive" Content="サブフォルダーも再帰的に比較(_R)"/>
                <CheckBox x:Name="ShowSame" Content="同一の項目も表示する(_I)"/>
            </StackPanel>
        </GroupBox>

        <TextBlock Foreground="{DynamicResource Fg.Subtle}" FontSize="11" Margin="2,10,2,0" TextWrapping="Wrap"
                   Text="変更ファイルの行をクリックすると 2ファイル差分を開きます。"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="比較(_O)" Width="90" IsDefault="True" Click="Ok_Click" Margin="0,0,8,0"/>
            <Button Content="キャンセル" Width="90" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
