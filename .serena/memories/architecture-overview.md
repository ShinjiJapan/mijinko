# Filer — だいなファイラー風2画面ファイラー

Windows専用。**WPF + C#/.NET 8**。VSCode開発前提(XAMLは手書き、`dotnet` CLI完結)。

## ソリューション構成
- `Filer.sln`
- `src/Filer.Core/` — UI非依存のコアロジック(net8.0)。TDD対象。依存: Markdig。
- `src/Filer.App/` — WPFアプリ(net8.0-windows)。MVVM(CommunityToolkit.Mvvm)。依存: Microsoft.Web.WebView2。
- `tests/Filer.Core.Tests/` — xUnit(139件)。

## Core層(src/Filer.Core)
- `FileEntry` (record) — Name/FullPath/IsDirectory/Size/LastModified。`IsParent`("..")、`IsArchive`(init, .zip)。`FileEntry.Parent(parentFullPath)`。
- `IDirectoryReader` / `DirectoryLister` — 抽象と実FS実装。先頭"..", dir(名前順), file(名前順)。fileの.zipは`IsArchive=true`。
- `ArchiveAwareReader : IDirectoryReader` — FSラッパ。`ArchivePath.TrySplit`でパスが`.zip`実ファイル境界の内側かを判定し、内側ならZIP(System.IO.Compression.ZipFile.OpenRead)を列挙、外側は内部FSリーダーへ委譲。仮想パス`…\x.zip\inner\dir`。`..`は書庫ルート→FS親フォルダー、内部→上位書庫内ディレクトリ。サブディレクトリはZIPエントリ名から合成。`ArchivePath.IsInsideArchive(path)`=`TrySplit`がtrueかつ`entryPath`非空(=書庫の内部)。書庫ファイル自体(`entryPath`空)は実ファイルなので**false**。MainViewModelでは格納先判定(コピー先/移動先/圧縮先/フォルダー作成/ツール起動)は`EnsureNotInArchive`(=TrySplit、書庫ルートも拒否)、対象アイテム判定(削除/移動/圧縮/リネーム/関連付け起動)は`EnsureNotInsideArchive`(=IsInsideArchive、zip自体は許可)を使い分ける。
- `ArchiveExtractor`(static) — 書庫内エントリの`ReadEntryBytes(virtualPath)`(バイト列読み)と`ExtractTo(virtualPath, destDir)`(実フォルダーへ抽出。ディレクトリは再帰、同名既存は拒否)。コピー/プレビューで使用。
- `ZipArchiver`(static) — 実ファイル/フォルダーを新規ZIP書庫へ圧縮(**Xキー**)。`Create(sourcePaths, destZipPath)`=各ソースを書庫ルート直下に元名で格納(フォルダーは再帰、空フォルダーはディレクトリエントリで保持。CompressionLevel.Optimal)、対象0件はInvalidOperationException・圧縮先既存はIOExceptionで拒否(フォールバックしない)。`DefaultZipName(firstSourceName)`=既定の圧縮後名(元拡張子を無視し常に`.zip`。例 report.docx→report.zip)。テスト9件。既存zipの書き換えではなく新規作成のみ(MainViewModel.CompressTargets→EnsureNotInArchiveで書庫内ソース/書庫内フォルダーは拒否)。
- `EntrySorter`(static)+`SortKey`{Name,Extension,Date,Size} — `Sort(entries,key,descending)`で並べ替え。親".."先頭→ディレクトリ群→ファイル群の順を保ち各群内をキーで整列(同値は名前順で安定)。
- `PaneTabs` — 1ペイン内の複数タブ管理。各タブは独立した`PaneState`。`Tabs`/`ActiveIndex`/`Active`/`TabPaths`(全タブの現在パス、保存用)。`AddTab`(アクティブタブの現在パスを複製しアクティブ直後へ挿入してアクティブ化)/`CloseActive`/`CloseTab(index)`(最後の1枚は残す。アクティブより前を閉じたら追従)/`Activate(index)`/`ActivateNext`/`ActivatePrev`(端でループ)。コンストラクタは`(reader, initialPath)`と復元用`(reader, IReadOnlyList<string> paths, int activeIndex)`(各パス1タブ、activeIndexクランプ、空paths例外)。PaneViewModelが保持。
- `PaneState` — 1ペイン状態+ナビ。`Open`はIsDirectory||IsArchiveで移動(通常ファイルは無視)。`GoToParent`/`NavigateTo`/`Reload`/`MoveCursor(To)`/`MoveCursorWrap`(端で反対側へループ。↑↓1行移動用)/`MarkedOrCurrent`/`TargetFolderPath`/`SelectedItemPath`。`SortKey`/`SortDescending`(既定:名前順・昇順)、`SetSort(key,descending)`(カーソル項目を保ったまま並べ替え)。**フィルター表示**: `_allEntries`(フィルター前の全件)とフィルター後の`Entries`を分離。`Filter`/`HasFilter`/`SetFilter(pattern)`(空=解除。`FileSearcher.TryCreateMatcher`(useRegex:false)で照合=`*?`は名前全体ワイルドカード/それ以外は部分一致大小無視。一致項目のみ表示し`..`は常に残す。可能ならカーソルを同項目に維持・消えたら先頭)。`ApplyFilter`が`_allEntries`→`Entries`を生成。**フォルダー移動(Load)でフィルターは自動解除**、Reload/SetSortはフィルター維持。マーク・MarkedOrCurrent・ToggleMarkAll等は表示中(フィルター後)の`Entries`に作用するためマーク→一括操作と組み合わせられる。Load/Reload/SetSortで`EntrySorter.Sort`適用。Reloadはカーソルを同じ項目(パス)に追従(IndexOfPath、消えていれば元indexをクランプ)。MainWindowのF5は`ReloadActivePreservingScroll`: 再読込前のListView内ScrollViewerの`VerticalOffset`を保存→Reload→`DispatcherPriority.Loaded`で`ScrollToVerticalOffset`復元＋実体化済み選択行(なければリスト)へフォーカス(スクロール位置は変えない)。`FindVisualChild<T>`でScrollViewerを取得。**フォルダーごとのカーソル位置記憶**: `_cursorMemory`(path→最後のカーソル項目名、OrdinalIgnoreCase)。`Load`冒頭で移動前フォルダーのカーソル名を`RememberCursor`(".."と空一覧は除外)、移動先は「記憶優先→明示指定(親戻り等)→先頭」で復元。セッション中のみ・永続化なし。
- `FileOperations` — Copy/Move/Delete/Rename/CreateDirectory(parentDir,name)。自己上書き・配下コピー・既存名リネーム・同名フォルダー作成はIOExceptionで拒否(フォールバックしない)。
- `FavoritesStore`+`FavoriteNode(Path,Label,Children?)` — お気に入り**階層ツリー**(項目=Path+Label/グループ=Label+Children)のJSON永続化。グループは「仕事/CLI」形式のグループパス指定(自動作成・同名統合・名前に/不可)、項目パスはツリー全体で重複排除。GetTree/GetGroupPaths/Add(path,label,group)→bool/Update(old,new,label,group)(重複先は無操作)/Remove/RenameGroup→bool/RemoveGroup/JoinGroup(static)。旧形式(パス文字列配列・フラットオブジェクト配列)は読込時ルート直下項目として自動移行し次回Saveで新形式へ。日本語は非エスケープ保存(UnsafeRelaxedJsonEscaping)。詳細は `mem:features-drive-and-favorites`。
- `HistoryStore` — 開いたフォルダー履歴(MRU)のJSON永続化。`Add(path)`(既存は先頭へ移動・大小無視・上限超過で末尾破棄・空無視)/`GetAll()`(新しい順)。既定上限50件。`%APPDATA%\Filer\history.json`。
- `DriveLister`/`DriveItem`/`IDriveProvider` — ドライブ列挙。
- `FilePreview`(static)+`PreviewKind`{None,Image,Text,Markdown,Pdf,Html} — 拡張子→プレビュー種別。.md/.markdown=Markdown、.pdf=Pdf(WebView2組み込みビューア)、.html/.htm/.xhtml/.mht/.mhtml/.svg=Html(WebView2レンダリング。S キーでソース表示と切替)。
- `TerminalProfiles`(static)+`TerminalProfile`{Name,ExePath,Arguments} — 利用可能シェル検出(PowerShell/PowerShell 7/コマンド プロンプト/Git Bash。既知インストール先を探索、fileExists差替でテスト可。先頭=既定シェル)。
- `TerminalPreferenceStore`+`TerminalPreference`{LastProfileName} — 前回開いたシェル種別名を`%APPDATA%\Filer\terminal-prefs.json`へ永続化(破損時null)。次回T/＋の既定シェルに使う。テスト4件。
- `ConPtySession` — Windows疑似コンソール(ConPTY/CreatePseudoConsole)で1シェルプロセスを実行。Output(UTF-8逐次デコード、VT込み)/Exited(残出力処理後に発火)イベント、WriteInput/Resize/Dispose(強制終了)。**STARTF_USESTDHANDLES+null stdハンドル必須**=コンソール親から起動時に親コンソールへ出力が漏れる問題の遮断(Windows Terminal同様の対策。これが無いとテスト/VSCodeターミナル配下で出力消失)。終了時は出力が200ms静かになるまで(最大2秒)待ってからClosePseudoConsole(早すぎると最終出力が落ちる)。
- `FileSearcher`(static)+`FileSearchOptions`(record Pattern/BaseDirectory+UseRegex/IncludeFiles=true/IncludeDirectories/SearchArchives)+`FileNameMatcher`(delegate, span受け) — **ファイル検索(F)**の本体。`TryCreateMatcher`(空=全一致/`*?`=名前全体ワイルドカード/それ以外=部分一致大小無視/useRegex=正規表現。不正regexはfalse+error)、`Search(options, token, onBatch?)`=基準ディレクトリ配下を再帰検索し相対パス(Name)順の`FileEntry`一覧を返す。**並列走査**: ConcurrentQueue+pendingカウンタでディレクトリ単位をワーカー(CPU数, 2..16)が分担、`FileSystemEnumerable`+ShouldIncludePredicate(span一致)で不一致ファイルの文字列生成を省く低アロケーション列挙。リパースポイントは潜らない・走査中消失ディレクトリはスキップ・キャンセルは例外なしで部分結果。SearchArchives=zip内エントリも照合(仮想パス`zip\inner`、壊れたzipは読み飛ばし)。onBatchはディレクトリ単位・**ワーカースレッドから並行に**呼ばれる。Name=基準ディレクトリ相対パス/FullPath=絶対(zip内は仮想)。実測: 約500万ファイル走査7.6秒(ウォーム)。`Search`は`SearchWithInfo`のラッパー。**`SearchWithInfo`**=`FileSearchResult`(Entries/`FileSearchEngine`{DirectoryScan,MftIndex}/EngineNote)を返す。`FileSearchOptions.PreferMft`(既定true)で**NTFS MFT直読み(Everything方式)**を優先し、不可なら通常走査+理由をEngineNoteへ(暗黙フォールバック禁止→UIに明示)。テスト16件。
- **MFT検索(Everything方式)**: `MftVolumeIndex`+`MftSearchService`+`UsnInterop`。**管理者権限が必要**(\\.\C: ボリュームオープン)。`MftVolumeIndex`=1ボリュームの索引。FRN(上位16bitシーケンス+下位48bitレコード番号)→レコード番号添字の並列配列(parent/seq/flags/名前ヒープ位置)+UTF-8名前ヒープ(1MBチャンク)で低メモリ。**ハードリンク対応**=`_extraNames`辞書(recno→追加(親FRN,名前)一覧。pnpm等が多用。ディレクトリはNTFS仕様上リンク不可)。`Set`(置換・追加名破棄)/`AddName`(累積。全構築用)/`Remove`/`PrimaryPathOf`/`Scan(matcher,needZips,baseFrn,...)`=全レコード走査→名前一致(span)→祖先ウォークでスコープ判定(キャッシュ付)→親パス構築(dirパスキャッシュ)→通知。シーケンス照合で再利用レコードの古い参照を弾く。I/Oなしで合成レコードテスト可(21件)。`MftSearchService`=静的ボリュームキャッシュ。全構築は**FSCTL_QUERY_FILE_LAYOUT**(INCLUDE_NAMESで全名前=ハードリンク含む。DOSのみ8.3名(flags==2)はスキップ。オフセットはエントリ相対)、非対応時はFSCTL_ENUM_USN_DATA(1ファイル1名前、note付き)。2回目以降は**FSCTL_READ_USN_JOURNAL差分適用**(FILE_DELETE→Remove/HARD_LINK_CHANGE→FRN収集し`FindFirstFileNameW`で名前一覧を取り直して再登録(レコード単体では増減判別不可のため)/他→Set。JournalID不一致・パージ済みは全再構築)。実体化=候補パスを`GetFileAttributesExW`並列stat(消えた/アクセス不可はスキップ=IgnoreInaccessible相当)+zip内は共用`FileSearcher.ScanArchiveEntries`。実測(C:全体約800万レコード): 初回構築約13秒/以降は差分0.3秒+プロファイル全体検索1.4秒(通常走査10.5秒の約7.5倍)。走査エンジンと結果完全一致を検証済み。非管理者・非NTFS・UNCではEngineNoteに理由を返し通常走査。テスト14件。
- `SearchResultsReader : IDirectoryReader` — 検索結果の**仮想一覧**をペイン表示するFSリーダーのラッパー(MainViewModelが`new SearchResultsReader(new ArchiveAwareReader(new DirectoryLister()))`で両ペイン共有)。`Register(label, baseDir, entries)`→`search://{id}/{label}`形式の仮想パス。`Read`は仮想パスなら先頭".."(FullPath=基準ディレクトリ)+登録エントリ(**実在しなくなった項目は除外**=F5で消える。zip内はTrySplitで書庫実在のみ確認)、通常パスは内部リーダーへ委譲。`IsVirtual`/`TryGetLabel`(static)。仮想パスは実在しないためセッション保存(Directory.Exists)から自然に外れ、履歴はMainViewModel側でIsVirtualをスキップ。PaneViewModelのパンくず=ラベル1区切り・タブ見出し=ラベル。テスト7件。内容検索(grep)の結果(一致ファイル)もそのまま乗る。
- `ContentSearcher`(static)+`ContentSearchOptions`(record Query/BaseDirectory+NamePattern/NameUseRegex/UseRegex/CaseSensitive(既定false=大小無視)/MaxFileSize=32MB/MaxMatchesPerFile=100/MaxLineLength=1000)+`ContentMatch`(Entry:FileEntry+Lines:`ContentMatchLine`(LineNumber1始まり/Text切り詰め済み))+`ContentSearchResult`(Matches/Engine/EngineNote)+`LineMatcher`(delegate) — **ファイル内容検索(grep)**。Fダイアログの「内容(grep)」欄に文字があると起動。`TryCreateLineMatcher`(空=エラー/useRegex=正規表現(不正はerror)/それ以外は部分一致・caseSensitiveで大小区別切替)。`SearchWithInfo`(=`Search`の実体)は①`FileSearcher.SearchWithInfo`(NamePatternでファイル絞り込み・IncludeFilesのみ・PreferMft=false)で候補列挙→②各候補を`Parallel.ForEach`(並列度CPU2..8)で`GrepFile`。GrepFileはサイズ超過/開けない/バイナリ(先頭8KBにNUL。UTF-16 BOMは除外)を読み飛ばし。**エンコーディング判定**=先頭64KBサンプルで`DetectEncoding`(BOMがあればUTF-8/16/32、無ければ`IsValidUtf8`(末尾の途中切れは許容・純ASCIIは真)が真ならUTF-8/偽ならShift-JIS(CP932。`CodePagesEncodingProvider`登録。net10はフレームワーク提供で追加パッケージ不要))→`StreamReader`(検出エンコーディング+BOM自動判定)で1行ずつ照合し行番号付きで収集(MaxMatchesPerFileで打ち切り)。onMatchはファイル一致ごとに**ワーカースレッドから並行に**呼ばれる。キャンセルは例外なし部分結果(OperationCanceledExceptionを飲む)。Matchesは相対Name順。**書庫内grepは非対応**。Shift-JIS/UTF-8(BOM有無)/UTF-16(BOM有)の日本語を自動判定して検索可。BOM無しUTF-16(NULでバイナリ扱い)・EUC-JP等は非対応。テスト23件。
- `IncrementalSearch`(static) — インクリメンタルサーチの一致検索。`FindFrom(entries,query,prefixOnly,startIndex)`=起点から下方向(起点含む・回り込み)で最初の一致、`FindNext(entries,query,prefixOnly,currentIndex,backward)`=現在位置を除く次/前の一致(回り込み)。大小無視・".."除外・空クエリ-1。prefixOnly=前方一致/false=部分一致。テスト16件。
- `BulkObservableCollection<T>` — 一括差し替え(`ReplaceAll`)/一括追加(`AddRange`)を1回のReset通知で行うObservableCollection。バインド済み一覧へ大量件数を逐次Addすると件数分の変更通知でUIスレッドが固まる問題(大量ファイルフォルダを開くとキー操作可能まで長時間待たされる)の対策。PaneViewModel.Entries(ペイン一覧)とFileSearchDialogの結果一覧(旧private RangeCollectionを置換・統合)が使用。テスト5件。
- `ListChunking`(static) — `FirstChunkCount(totalCount, cursorIndex, chunkSize)`=大量件数一覧の**2段階表示**(先頭チャンク即表示→残りを直後に一括追加)の判定。件数がチャンク2倍以下 or カーソル復元位置がチャンク外なら全件(分割なし)。テスト6件。
- `ColumnWidthCandidates`(static) — `Select(values, count=3)`=Auto幅カラム調整で実測すべき候補文字列を表示幅スコア(半角=1/全角=2)上位から選ぶ。全行FormattedText実測(数万件で数百ms)の回避用。MainWindow.RefreshColumnsが使用。テスト7件。
- `FileNameParts`(static) — `Split(fileName)`で名前/拡張子に分割。拡張子は「先頭以外の最後のドット」以降。先頭ドットのみ(.gitignore等)は拡張子なし。EntryViewModelの名前/拡張子列が使用(Path.GetExtensionだと.gitignoreが拡張子扱いされる不具合を修正)。
- `GitStatus.cs`: `GitEntryState`(enum None/Untracked/Added/Modified/Conflicted。値=ディレクトリ集約優先度)+`GitStatusSnapshot`(Branch/Ahead/Behind/`BranchDisplay`/`StateOf(relPath,isDir)`)+`GitStatusParser.Parse`(`git status --porcelain=v2 --branch -z` 解析。種別1/2/u/?、祖先ディレクトリへ状態集約)+`GitRepositoryLocator.FindRoot`。App側 `GitStatusService.QueryAsync`(git.exe 起動)が `PaneViewModel.RefreshGitStatus`(Refresh末尾・非同期・世代管理)から呼ばれ `EntryViewModel.GitState`/`PaneViewModel.GitBranchText` を更新→名前列のバッジ(M/A/?/!、テーマ非依存の固定色 Border)とパンくず右端ブランチ表示。詳細は `mem:features-git-status`。\n- `PathBreadcrumb`(static)+`BreadcrumbSegment`(record Name/Path) — `Build(path)`で絶対パスを「ルート→各階層」の累積パンくず列へ分割。ルートはドライブ`G:\`/UNC`\\server\share`、以降は区切り単位。書庫(.zip)仮想パスも同様に分割。MainWindowのパス表示(エクスプローラー風パンくず。各区切りクリックでそのフォルダーへ移動)が使用。
- `MarkdownRenderer`(static) — Markdig(UseAdvancedExtensions)でMD→ダークテーマHTML文書(`ToHtmlDocument`/`RenderBodyHtml`)。```mermaidフェンスは内部`MermaidCodeBlockRenderer`で`<div class="mermaid-fig">`(ツールバー＋`<pre class="mermaid">`)へ変換(本文はWriteEscape)。HTML末尾の`Script`定数: 同梱mermaid.min.js参照→`mermaid.initialize({startOnLoad:false})`＋`mermaid.run`→各.mermaid-figのsvgを枠幅へ自動フィット(`fitScale=min(1,枠幅/自然幅)`で初期表示、拡大はしない)、図ごとにツールバー(data-act=out/reset/in/full。resetは100%実寸)で拡大/縮小/全画面(requestFullscreen)を配線、fullscreenchangeで再フィット。本文は全幅(.markdown-bodyのmax-width制限なし)。keydownでEsc/Enterを`window.chrome.webview.postMessage('close')`(全画面中は解除優先でスキップ)。
- `SessionStore`+`SessionState`(Left/Right=`SessionPane`, IsLeftActive, `Window`=`WindowBounds?`)+`SessionPane`(TabPaths/ActiveTabIndex)+`WindowBounds`(Left/Top/Width/Height/Maximized) — 終了時のタブ構成とウィンドウ位置・サイズをJSON永続化。`Load`は未保存/破損(JsonException)/旧形式(Left or Rightがnull=タブ情報なし)時はnull→既定値起動。`Window`はオプション(旧形式JSONはnull→既定サイズ起動)。ウィンドウ復元は`App.OnStartup`の`ApplyWindowBounds`(仮想画面と交差しない位置は捨てる/サイズは正値時のみ適用/最大化復元)・終了時`CaptureWindowBounds`(最大化/最小化中は`RestoreBounds`で通常サイズ保存。ただし`RestoreBounds`が未確定で`Rect.Empty`(=Infinity)の場合は`IsFinite`判定でLeft/Top/Width/Heightへフォールバック=InfinityはJSON化不可でSave全体が例外になり最大化フラグが永続化されない不具合を回避)。`MainViewModel.CaptureSession(WindowBounds? window=null)`がUIから受け取る。
- `KeyChord`(struct) — 「Ctrl+Shift+T」形式のジェスチャ文字列の解析/正規化/表示変換。`TryParse`(修飾Ctrl/Control・Shift・Alt、キー名はWPF Key名+エイリアスReturn→Enter/Esc/BS/PgUp等を標準形へ、1文字英数字は大文字化/D付与)。`ToString`=正規形(Ctrl→Shift→Alt順)、`Normalized`=大文字比較キー、`DisplayText`=表示用(D1→1, NumPad1→Num1, Left→←, Back→BS, Delete→Del等)。テスト9件。
- `KeyBindings.cs`: `KeyBindingAction`(record Id/DisplayName/Category/DefaultGestures/HelpLabel)+`KeyBindingActions.All`(組み込みアクション定義。**定義順=フッター表示順**。HelpLabel=nullはフッター非表示。外部ツールは含めない=`ForTool(tool)`で`tool:<Id>`の動的アクション化/`ToolIdOf`/`ToolPrefix`)+`KeyBindingMap`(組み込み+settings.json上書き+**動的ツールアクション**から対応表構築。`Build(overrides)`と`Build(overrides, dynamicActions)`/`Actions`(有効一覧=組み込み+ツール)/`GesturesFor`/`TryResolve`/`OwnerOf`/`Replace`/`ResetToDefault`/`ToOverrides`(既定と異なる**組み込み**のみ。`tool:`除外)。明示指定(上書き+ツール)ジェスチャは非明示アクションの既定から奪う。空=割当なし、不正無視、1ジェスチャ1アクションをReindexで保証=`_actions`定義順先勝ち)+`KeyHelp`(map.Actionsから動的生成)。テスト23件。**注意: 組み込みにツール用id(tool.skimdown等)を残すと孤児バインド・設定二重行**。
- `ExternalTool.cs`(record Id/Label/Kind=Executable|StoreApp/Target/Arguments/Gestures)+`ExternalTools.Defaults()`(既定4ツール=VSCode/Terminal/GitBash/SkimDown)。`ToolMacros.cs`=`ToolMacroExpander`(引数マクロ$F/$W/$E/$P/$O/$L/$R/$MS/$MF/$MO/$mO展開、$MO空でnull=キャンセル)+`ToolMacroContext`。詳細は`mem:features-external-tools`。
- `AppSettingsStore`+`AppSettings`(KeyBindingOverrides+`IReadOnlyList<ExternalTool> Tools`+Theme+`bool LightweightListAutomation`=false) — JSON永続化(`%APPDATA%\Filer\settings.json`、camelCase、JsonStringEnumConverter、日本語非エスケープ)。Loadは未保存/破損→既定(4ツール)、`externalTools`キー無し→既定補完・空配列→ツール無し尊重、`lightweightListAutomation`キー無し→false。Saveはキー差分のみ+ツール一覧全体。テスト13件。
- **UIA軽量化設定**(`LightweightListAutomation`): ON=ペイン一覧の行をUIAの子として公開しない(`PaneListView`参照)。設定ダイアログ「外観」タブ末尾の「パフォーマンス」チェックボックス。既定OFF=従来動作(スクリーンリーダー/UIA行列挙対応)。OKで即時反映(静的フラグをピアが動的参照、再起動不要)。適用箇所=App.OnStartup(起動時)とMainWindow.ShowSettingsDialog(OK時)。実測(常駐UIAクライアントあり環境): ON時はフォルダー移動の全件表示が7千件 約100〜130ms/4.6万件 約140〜300ms(OFF時500〜650ms)。**ONでもSelectionPattern.GetSelection()は動作**(選択行ピアはオンデマンド生成)=verify-downkey.ps1のUIA検証は両モードで使用可。失われるのは全行の列挙(FindAll・スクリーンリーダーの行読み上げ)のみ。

## App層(src/Filer.App)
- `Controls/PaneListView` — ペイン一覧用ListView派生。`OnCreateAutomationPeer`が`ListViewAutomationPeer`派生の独自ピアを返し、`static bool LightweightAutomation`がONのとき`GetChildrenCore`でnull(行を子として列挙しない)。XAMLは`local:PaneListView`(LeftList/RightList)。`PaneListStyle`(TargetType=ListView)は明示Style指定なので派生型にも適用される。
- `ViewModels/EntryViewModel` — 表示ラッパ。DisplayName/DisplayExtension/DisplaySize/DisplayDate/IsMarked/`IconImage`(ShellIconProvider)。`Entry`公開。
- `ShellIconProvider`(static) — SHGetFileInfoでシェルアイコン。拡張子単位キャッシュ、exe/lnk/ico等は実ファイル固有、ZIP内/非実在はUSEFILEATTRIBUTES。CreateBitmapSourceFromHIcon→Freeze→DestroyIcon。
- `ViewModels/PaneViewModel` — 1ペインの表示。`Entries`は`BulkObservableCollection<EntryViewModel>`(Refreshで`ReplaceAll`=Reset一括通知)。**2段階表示**: Open/NavigateTo/GoToParentは`Refresh(chunked:true)`で先頭128件だけ即表示→残りはDispatcher(Loaded優先度)で一括追加(`_refreshGeneration`で移動が重なったら破棄。キー/マウス入力はInput優先度=追加完了後に処理されるため部分表示を見ない)。**Reloadはchunkなし**(直後に`Entries`を同期参照するPreviewWindowの画像送りがあるため)。`EntryCount`=".."込み全件数(StatusTextの分母。2段階表示中の部分集合に左右されない)。`RunTimed`=環境変数`FILER_PERFLOG`にパス指定でフォルダー移動の readSort/refresh 時間をログ(MainWindow側は paint/layout/renderIdle を追記)。内部に`PaneTabs`を保持しアクティブタブの`PaneState`を投影(`_state => _tabs.Active`)。復元用コンストラクタ`(reader, IReadOnlyList<string> tabPaths, int activeTabIndex)`と`TabPaths`(保存用)を持つ。`SelectedIndex`(two-way)、`MoveCursor(To)`、`DirectoryPath`/`SelectedItemPath`、`Current`(FileEntry)。`Breadcrumb`(ObservableCollection<BreadcrumbSegment>。Refreshで`PathBreadcrumb.Build`から再生成)。**タブ**: `Tabs`(ObservableCollection<TabViewModel>)/`ActiveTabIndex`(two-way。タブ列の選択と双方向)。`AddTab`/`CloseActiveTab`/`CloseTab(TabViewModel)`/`ActivateNextTab`/`ActivatePrevTab`。`RebuildTabs`がPaneTabsから見出しを再生成、`_suppressTabSync`でバインディング再入を抑止。Refreshでアクティブタブ見出しを現在フォルダ名(`PathBreadcrumb`末尾)へ更新。
- `ViewModels/TabViewModel` — タブ1枚の見出し(`Title`、フォルダー名)。実ナビ状態はPaneTabs側のPaneStateが持つ。
- `ViewModels/MainViewModel` — 2ペイン管理。コンストラクタは`(SessionPane left, SessionPane right, bool isLeftActive, favoritesFilePath, historyFilePath, settingsFilePath)`でタブ構成と設定(`AppSettingsStore.Load`→`Settings`公開)を復元。readerは`new ArchiveAwareReader(new DirectoryLister())`。Copyは書庫内項目を`ArchiveExtractor.ExtractTo`で抽出。`EnsureNotInArchive`で書庫書き換え系(Move/Delete/Rename/書庫内コピー/外部ツール)をIOExceptionで拒否。**外部ツール**: `LaunchTool(toolId)`(`Settings.Tools`からツール検索→`BuildMacroContext`で引数マクロ文脈構築→`_tools.Launch`)。`BuildMacroContext`は$F=カーソル名/$P等=各窓パス(TrimDir)/$MS・$MF=`pane.Targets`(空はフォルダーfallback)/$MO・$mO=他方`pane.Marked`。`UpdateSettings(settings)`で保存+読直し。`OpenSelectedWithAssociation`(Shift+Enter)は残存。**ZIP圧縮**: `CompressTargets(zipFileName)`(X。`Active.Targets`(マークorカーソル)を`ZipArchiver.Create`で`Active.DirectoryPath`直下のzipへ。拡張子無しは`.zip`補完=`EnsureZipExtension`。圧縮先フォルダー・各ソースが書庫内なら`EnsureNotInArchive`で拒否)。MainWindow側は`CompressInteractive`(X。先頭Targetから`ZipArchiver.DefaultZipName`で既定名を作りInputDialog表示→`Run(Vm.CompressTargets)`)。`CaptureSession`は各ペインの`TabPaths`/`ActiveTabIndex`を`SessionPane`へ。`HistoryStore`を保持し`OnPanePropertyChanged`でどちらのペインの`CurrentPath`変更(タブ切替含む)も履歴へ`Add`。`GetHistory()`公開。
- `KeyChordWpf`(static) — Core `KeyChord` ⇔ WPF Key/ModifierKeys 相互変換。`TryToWpf`(ジェスチャ→Key+Modifiers)/`FromKeyEvent`(キャプチャ用。Return→Enter/Prior→PageUp/Next→PageDown/Capital→CapsLockを明示変換=enum別名のToString不定対策。修飾単独・None/ImeProcessed/DeadCharProcessedはnull)/`IsModifier`。
- `SettingsDialog.xaml(.cs)` — **設定UI(Z)**。タブ2枚。①キー割り当て: `_map.Actions`(組み込み+外部ツール)をカテゴリ別ListViewグループ表示(操作/キー/変更●)+検索フィルタ。「キーを変更...」(行ダブルクリック/Enter可)→キャプチャモード(CaptureBar、OnPreviewKeyDownで全キー消費、押下キー+修飾を割当、Esc/Deactivatedで中止)。追加/解除(Delete可)/既定に戻す/すべて既定に戻す。重複は確認→相手から奪う。②外部ツール: ListView(ラベル/種類/対象/キー)+追加/編集/複製/削除/↑↓。`_tools`が真実・`_map`は導出(`RebuildMap`=ToOverridesで組み込み上書き保ち再構築+`SyncToolGesturesFromMap`)。ツールから外れたキーは`RestoreDefaultOwners`で既定の組み込みへ復活。OKで`new AppSettings(_map.ToOverrides(), _tools)`を`Result`に。ダークテーマ内蔵。OKはIsDefaultにしない。
- `ToolEditDialog.xaml(.cs)` — 外部ツール1件編集。ラベル/種類ラジオ(実行ファイル/ストアアプリ)/対象(exe=パス+参照[OpenFileDialog]、store=AUMID+「アプリを選択...」[AppPickerDialog])/引数テンプレ(「マクロ一覧 ?」でMessageBox)/キー(キャプチャ・`ConflictLookup`で重複確認)。`ToolDraft`を返す(IdはSettingsDialogが`GenerateId`でスラッグ生成)。
- `AppPickerDialog.xaml(.cs)` — インストール済みアプリ選択。`InstalledAppLister.List()`(shell:AppsFolder/COM、STAスレッドで非同期列挙、281件規模)を検索付きListView表示。`SelectedApp`(InstalledApp=Name/Aumid)を返す。
- `MainWindow.xaml(.cs)` — 各ペイン上部にタブバー(`TabStripStyle`の横並びListBox+`AddTabButtonStyle`の`+`ボタン。各タブは`TabItemStyle`で見出し+`×`閉じるボタン。選択タブは一覧と同色#252526で繋がって見える)。`AddTab_Click`/`CloseTab_Click`(×。`Vm.Left.Tabs.Contains`で所属ペイン判定)/`TabStrip_PreviewMouseLeftButtonDown`(タブ列クリックで該当ペインをアクティブ化)。`ActivatePane(isLeft)`でアクティブ側切替を一元化(Breadcrumb_Click/Pane_GotKeyboardFocus/List_MouseDoubleClickも使用)。その下がパンくずバー(`ItemsControl`+`Breadcrumb`、`BreadcrumbItemTemplate`で各区切りを`BreadcrumbButtonStyle`ボタン+`›`区切り表示)。`Breadcrumb_Click`でそのペインをアクティブ化し`NavigateTo(segment.Path)`。`OnPreviewKeyDown`でキー振り分け。Enter(非Shift)とダブルクリック(`List_MouseDoubleClick`)は共通の`ActivateCurrent`: IsDirectory||IsArchive→Open、それ以外→`PreviewCurrent`(実ファイルor書庫内ファイル、種別None以外でPreviewWindow起動)。ダブルクリックは対象ペインをアクティブ化してから実行。←→はペイン位置で親移動/ペイン移動切替。Shift+Enterは関連付け起動。マウスドラッグでアプリ外へコピー出力。`EntryRowStyle`は選択背景なし・アクティブ側選択行のみ下線(`IsSelected`＋`DataContext.IsActive`で判定。OSフォーカス非依存=アプリ非アクティブでも下線維持。各ListViewの`DataContext`をペインVM=`Left`/`Right`にし`ItemsSource={Binding Entries}`/`SelectedIndex={Binding SelectedIndex}`)。
- `PreviewWindow.xaml(.cs)` — 全画面(WindowStyle=None/Maximized)。Grid Row1に画像(Image)/テキスト(TextBox)/Markdown(`wv2:WebView2 x:Name=MarkdownView`)を重ね、種別で表示切替。画像=BitmapImage(OnLoad/Freeze, Uniform。書庫内StreamSource=ReadEntryBytes)、テキスト=読み取り専用TextBox。表示種別は`_kind`(PreviewKind)で保持。テキスト表示中の↑↓/PgUp/PgDn/Home/Endは`TextView`の`LineUp/LineDown/PageUp/PageDown/ScrollToHome/ScrollToEnd`でスクロール(カーソル不動)。画像中の↑↓で`StepToAdjacentImage`(`NextImageIndex`/`TryShowImageAt`で実装。横並び時は2枚分進む)。4キーで進む/6キーで戻る(`StepToAdjacentImage`。1枚表示時1枚・2枚表示時2枚送り。↑↓も同じ。漫画の左右方向)。**1キー/Endで横並び2枚表示トグル**(`ToggleTwoUp`。漫画の右→左順=現在画像を右`ImageView`/次画像を左`ImageView2`。`ImagePanel`の2カラムGrid、左`SecondCol`幅を0⇔*で切替。2枚時は左画像右寄せ・右画像左寄せで中央の境目に隙間なく接する=1枚時はImageViewをStretchに戻す。ヘッダーは「左(次)|右(現在)」)。**画像プレビュー中のC/M/D**: コンストラクタで`MainViewModel`を受け取り(`_pane=main.Active`)、C=CopyToOther/M=MoveToOther/D・Delete=DeleteTargetsを**確認なし**で実行(`RunOp`がtry/catch、失敗時のみMessageBox)。移動・削除後は`ShowNearestImageOrClose`でカーソル位置以降→以前の順に次画像へ送る/無ければ閉じる。F5でペイン再読込(`_pane.Reload`)→`ShowNearestImageOrClose`で現在画像を再表示/消えていれば次へor閉じる。**F/F1キー**(`CyclePreviewView`): 全画面(WindowState=Maximized)⇔**反対側ペイン**領域内の縮小表示をトグル(自身の一覧を見ながらプレビューするため。MainWindow.PreviewCurrentが非アクティブ側の`LeftPane`/`RightPane` Borderを渡す)。ペイン領域要素の`PointToScreen`矩形を`TransformFromDevice`でDIP化し`WindowState=Normal`で配置。反対側が非表示(F1の1ペイン全画面中)なら`GetPaneRegionRect`がnull→全画面のまま。MarkdownはWebView2がキーを奪うため不可。**Markdown**: `LoadMarkdownAsync`がMD読込(書庫内ReadEntryBytes)→`MarkdownRenderer.ToHtmlDocument`→`%LOCALAPPDATA%\Filer\preview`へpage-{guid}.html書出し(`CleanupOldPages`で旧page掃除)＋埋め込みmermaid.min.jsを`EnsureMermaidScript`展開→`CoreWebView2Environment.CreateAsync`(userDataFolder=`%LOCALAPPDATA%\Filer\WebView2`)→`SetVirtualHostNameToFolderMapping("filer.preview", previewDir, Allow)`→`Navigate("https://filer.preview/page-…html")`。`WebMessageReceived`で'close'受信→閉じる。`NavigationCompleted`で`MarkdownView.Focus()`しブラウザのネイティブキースクロール(↑↓/PgUp/PgDn/Home/End)を有効化。WebView2初期化失敗(ランタイム未導入)はフォールバックせずMessageBox通知。Esc/Enterで閉じる。**PDF**: `LoadPdfAsync`がPDFを`%LOCALAPPDATA%\Filer\preview`へdoc-{guid}.pdf書出し(実ファイルはFile.Copy/書庫内はReadEntryBytes、`CleanupOldDocs`で旧doc掃除)→MarkdownViewを流用しfiler.preview経由でNavigate→WebView2組み込みPDFビューアで表示。WebViewへフォーカスを移さずウィンドウ側のEsc/Enter終了を維持。スクロールは↑↓/PgUp/PgDn/Home/End(`ScrollPdf`がDevToolsプロトコル`Input.dispatchKeyEvent`でPDFビューアへキー送出。フォーカスを奪わずEsc/Enter終了と両立)・マウスホイール・ビューアのツールバー。WPFのWebView2にはAcceleratorKeyPressed相当の公開イベントが無いためこの方式。
- `Assets/mermaid.min.js` — mermaid.js本体(EmbeddedResource。csproj `<EmbeddedResource Include="Assets\mermaid.min.js" />`)。リソース名は末尾一致で解決。
- `Assets/appicon.ico` — アプリアイコン(かわいいフォルダーのマスコット。水色背景＋まんまる目・ほっぺ)。csproj `<ApplicationIcon>`(exe/エクスプローラー)＋`<Resource>`、MainWindow `Icon="Assets/appicon.ico"`(タイトルバー/タスクバー)。生成スクリプトは`agent/tmp/make_icon.py`(Pillowでスーパーサンプリング描画→マルチサイズico)。
- `App.xaml.cs` — `SessionStore`復元/保存。`SanitizePane`で保存タブのうち実在フォルダーのみ復元(全滅なら既定単一タブ)、activeIndexクランプ。`%APPDATA%\Filer\`にfavorites/session/history。
- `Terminal/` — **組み込みターミナル**(VSCode同様のxterm.js+ConPTY方式)。`TerminalAssets`=埋め込みxterm一式(Assets/xterm/xterm.min.js・xterm.css・xterm-addon-fit.min.js)+terminal.html(const)を`%LOCALAPPDATA%\Filer\terminal`へ展開。`TerminalSessionView`(Border)=タブ1枚分。WebView2でterminal.htmlを仮想ホスト(filer.terminal)表示し`ConPtySession`と橋渡し(JS→C#: ready/input/resize/focus-list のJSON postMessage、C#→JS: 文字列=端末出力term.write/JSON{type:focus}=フォーカス。出力はStringBuilderに溜めBeginInvokeでまとめ送出=ディスパッチ洪水防止。readyでResize→Start)。terminal.html内のCtrl+Tはシェルへ送らずfocus-list通知(ファイラー一覧へ戻る)、F1はcycle-view通知(表示形態切替。端末のF1は犠牲)。`TerminalPanel`(DockPanel)=タブ列(+:既定シェル(`defaultProfileProvider`=前回種別)で新タブ/˅:ContextMenuで種類選択/タブクリック切替/×クローズ。C#構築のBorderタブ)+セッションホストGrid。`ProfileOpened`でMainWindowが`TerminalPreferenceStore`へ種別保存。`FocusListRequested`(Ctrl+T)/`CycleViewRequested`(F1)をMainWindowへ中継。シェル終了(exit)でタブ自動クローズ、全タブ閉→AllTabsClosed。新タブはページready時にアクティブなら`FocusTerminal`(WebViewへ`_web.Focus()`+Keyboard.Focus+JS focus)。
- **ターミナル表示形態**(`MainWindow._terminalView`={OnePane,FullScreen}): 単一オーバーレイ`TerminalHost`(Border、3列Gridの最後・ZIndex5)に`TerminalPanel`を一度だけ`Child`設定。`ApplyTerminalView`で`Grid.Column`/`Grid.ColumnSpan`と可視性を切替えるだけ(**WebView2は再ペアレントしない**=表示切替で再初期化されない)。OnePane=非アクティブ側1列、FullScreen=全3列(両ペイン+スプリッター被覆)。`CycleTerminalView`(F1)でOnePane⇄FullScreenをトグル(常に端末へフォーカス)。隠す状態は廃止=ターミナルを畳むには全タブを閉じる(`OnTerminalAllClosed`→Collapsed)。`TerminalOpen`プロパティで状態判定。Tabキーは表示中なら一覧→端末フォーカス。

## F1: 通常⇄全画面トグル(全ビュー統一)
ファイル一覧・ターミナル・各プレビューで **F1 = 通常 ⇄ 全画面** をトグル(隠す状態は廃止。ユーザー選択でF1採用。端末のF1は犠牲)。
- **ファイル一覧**(`MainWindow.CyclePaneLayout`/`ApplyPaneLayout`/`_paneStep`{0通常,1アンカー全画面}): 3列Grid(`Col0`/`ColSplit`/`Col2`+`PaneSplitter`)の列幅を `*`/`0` 切替。アンカー=トグル開始時のアクティブ側。全画面時は表示側を`SetActivePaneFlags`+フォーカス。手動ペイン切替(`ActivatePane`)で`ResetPaneLayout`(50/50へ)。`OnPreviewKeyDown`のF1は `TerminalOpen?CycleTerminalView:CyclePaneLayout`。
- **ターミナル**: 上記`CycleTerminalView`(OnePane⇄FullScreen)。一覧フォーカス時はWPFのF1、端末入力中はterminal.htmlがF1横取り→cycle-view通知。
- **プレビュー**(`PreviewWindow._view`{Maximized,PaneRegion}・`CyclePreviewView`/`ApplyPreviewView`): F/F1でトグル。Maximized=全画面/PaneRegion=**反対側ペイン**領域(`GetPaneRegionRect`。自身の一覧を見ながらプレビュー)。Markdownは`MarkdownView`がキーを奪うためHTML側keydownでF1→cycle-view postMessage、`OnWebViewMessage`が処理。PDFはWPFフォーカスのままなので`OnPreviewKeyDown`のF1で処理。CloseAll=アプリ終了時の全シェル強制終了(MainWindow.OnClosed)。WebView2環境は静的Task共有(userDataFolder=%LOCALAPPDATA%\Filer\WebView2)。
- 他: InputDialog/SelectionDialog/SortDialog/ExternalTools(ExternalToolLauncher=汎用Launch(tool,ctx)・マクロ展開・パス解決・StoreApp分岐 / InstalledAppLister=shell:AppsFolder列挙 / UwpLauncher)。アクティブペイン枠はMainWindowの`Style`+`DataTrigger`(`Left/Right.IsActive`→`Accent`)で強調(旧`ActiveBorderConverter`は廃止)。`SelectionDialog`(ドライブ・お気に入り・履歴共用。↑↓/Enter/Esc/**シングルクリック**で決定。`numbered`=先頭9件に1〜の番号で数字キー即決定/`letterSelect`=Value先頭文字=ドライブ文字キーで即決定(ドライブ選択)/`reload`+`onEdit`+`onDelete`=各項目に編集・削除ボタン(お気に入り。`FavoriteEditDialog`でラベル+グループ+パス編集(グループ変更=移動)、グループ行はInputDialogで名前変更・中身ごと削除確認。削除は確認後Remove。ボタンはアイコンのみ=Segoe MDL2 Assets鉛筆/ゴミ箱グリフ+ToolTip、`IconButtonStyle`)。一覧は`HorizontalScrollBarVisibility=Disabled`で横スクロールせず、ラベルは`*`列+`TextTrimming=CharacterEllipsis`で末尾「…」省略。お気に入り選択はnumbered:true)。`FavoriteEditDialog`(登録/編集兼用。ラベル+グループ(編集可能ComboBox=自由入力&▼で既存グループ選択・/区切りで階層作成。`Controls.xaml`の`DialogComboBox`テーマ追従スタイル使用)+パスの3フィールド。パス空はOK不可)。お気に入り選択はグループ階層ドリルダウン対応(グループ行=名前+›、Enter/→で中へ・←/BSで戻る・パンくず表示・番号は階層ごと)。`SortDialog`(ソート方法N/E/D/S・順番A/Rのラジオ。Enter決定/Escキャンセル。`SelectedKey`/`Descending`を返す。MainWindowのSキーで開きアクティブ側に`SetSort`適用)。一覧ヘッダー(名前/拡張子/サイズ/更新日時)クリックでもソート可(`MainWindow.ColumnHeader_Click`。`GridViewColumnHeader.Click`を両ListViewで購読→列番号を`ColumnSortKey`でSortKeyへ。同列再クリックで昇降トグル、別列は昇順から。クリック側ペインをアクティブ化)。ソート中の列ヘッダーに▲(昇順)/▼(降順)を表示(`ApplySortIndicators`を`FocusActiveList`で両ペイン更新)。

## キー操作(すべて設定(Z)で変更可能。以下は既定値)
**キーディスパッチ**: MainWindowの`OnPreviewKeyDown`は switch ではなく、`_keyToAction`((Key,ModifierKeys)→アクションId。`RebuildKeyBindings`が`Vm.Settings`の`KeyBindingMap`から構築、DataContextChanged・設定OK後に再構築)→`_actions`(アクションId→Action。`BuildActions`、全39アクション)の2段辞書引き。実キーは`Key.System`→SystemKey/`Key.ImeProcessed`→ImeProcessedKeyで解決。**メインウィンドウはIME無効**(`InputMethod.SetIsInputMethodEnabled(this,false)`。日本語入力モードだと英字キーがImeProcessedに化けるため。入力ダイアログは別WindowなのでIME可)。環境変数`FILER_KEYLOG`=パスでキーイベント調査ログ。フッターヘルプは`KeyHelp`で現在の割り当てから動的生成(設定変更に追従)。**Z:設定**(`ShowSettingsDialog`→`SettingsDialog`「キー割り当て/外部ツール/外観」3タブ→OKで`Vm.UpdateSettings`+`RebuildKeyBindings`+`ThemeManager.Apply`=再起動なし即時反映)。
Tab:ペイン切替 / Ctrl+T:新タブ(現在フォルダ複製) / Ctrl+W:タブを閉じる / Ctrl+→:次タブ / Ctrl+←:前タブ(端でループ。旧Ctrl+Tab/Ctrl+Shift+Tabは廃止) / Enter:開く(dir・ZIP移動/画像・テキスト・Markdownは全画面プレビュー) / ←:左=親/右=左へ / →:左=右へ/右=親 / BS:親 / Space:マーク&下 / C:コピー(確認なし) / M:移動(確認あり) / D・Del:削除(確認あり) / R:改名 / K:新規フォルダー作成(InputDialog) / **X:ZIP圧縮**(マークorカーソルの対象を現在フォルダー直下のzipへ。既定名=先頭項目名・元拡張子無視で常に`.zip`。InputDialogで名前指定・拡張子無しは`.zip`補完。`ZipArchiver`) / Shift+K:SkimDown(既定の外部ツール) / S:ソート(SortDialog。一覧ヘッダークリックでもソート可。同列再クリックで昇降トグル、ソート列に▲▼表示) / **E:インクリメンタルサーチ**(`IncrementalSearchDialog`。入力のたびに`IncrementalSearch.FindFrom`(起点=開いた時のカーソル位置)でアクティブ側カーソルを一致項目へ移動+ScrollActiveIntoView。「先頭から検索」チェック=前方一致⇔部分一致、↑↓キー/上方・下方検索ボタン=`FindNext`で次候補(ボタンはFocusable=Falseで入力継続)、一致なしは「一致なし」表示、クエリ空に戻すと起点へ復帰。Enter/閉じる=確定、Esc/×=起点位置へ戻す。ダイアログはオーナー右下寄り配置・ToolWindow。MainWindow.ShowIncrementalSearch+SnapshotEntries+MoveToMatch) / **F:ファイル検索**(だいな風。`FileSearchDialog`=検索ファイル名(正規表現チェック)+基準ディレクトリ+ファイル/ディレクトリ/圧縮ファイル内も検索の各チェック+検索開始(S)/転送して閉じる(T)/閉じる(C)/ジャンプ(J)。検索はTask.Run+`FileSearcher.SearchWithInfo`(使用エンジン+MFT不可理由を`EngineText`に表示。管理者起動ならMFT索引で高速)、発見バッチは150ms DispatcherTimerでまとめてListBoxへ(RangeCollection=Reset通知一括追加+仮想化でUI固まらず)、完了時にソート済み一覧へ差替。「N 個発見」逐次更新。転送=`Vm.RegisterSearchResults`→`NavigateActiveTo(search://…)`で仮想一覧表示(".."=検索時の基準ディレクトリへ。`SearchedBaseDirectory`を使用)、ジャンプ(ダブルクリック可)=項目のあるフォルダーへ移動しカーソル一致(`MainWindow.ShowFileSearchDialog`/`SearchBaseDirectory`/`IndexOfPath`)。検索中の転送/ジャンプは中断してその時点の結果を使用。**内容(grep)欄に文字を入れると内容検索**=`ContentSearcher`起動。ファイル名欄は対象ファイルの絞り込みに変わり、ディレクトリ/圧縮/高速検索ボタンは無効化、内容用に正規表現・大小区別チェックが有効化。結果は一致ファイル一覧+下部`PreviewPanel`に選択ファイルのマッチ行プレビュー(行番号付)、件数は「Nファイル/N行一致」。転送/ジャンプは名前検索と共通(`FileEntry`で`SearchResultsReader`に乗る)、転送ラベルは`PatternText`が「内容「語」名前「フィルタ」」を返す。`StartSearchAsync`が内容欄の有無で`RunContentSearchAsync`/`RunSearchAsync`に分岐) / **Shift+F:フィルター表示**(`FilterDialog`=絞り込みパターン入力。入力のたびに`Vm.Active.SetFilter`でアクティブ側を絞り込み(例 "*.jpg" だけ表示)、`PaneState`の`Filter`/`SetFilter`が`..`を残して一致項目のみ表示・一致件数を「N 件」表示。Enter/決定=確定、Esc/×=開いた時点のフィルターへ戻す・空欄で解除。**確定後は一覧上でEsc(`filter.clear`アクション、既定Escape)でフィルター解除**(`HasFilter`時のみ動作・無ければno-op)。パンくず横の表示は確定済みなら「絞り込み: xxx (ESCで解除)」・編集中(FilterEditing)は「絞り込み: xxx」(編集中のEscはダイアログのキャンセルなので解除ヒントは出さない)。フォルダー移動で自動解除。マーク→一括操作と併用可。パンくず横に「絞り込み: …」を`FilterTextStyle`で表示。`PaneViewModel.FilterEditing`(ShowFilterがダイアログ表示中trueにする)で**Shift+F押下の瞬間から空入力でも「絞り込み:」を表示**(`FilterText`は`HasFilter||FilterEditing`で出す。空かつ非編集時のみ畳む)。MainWindow.ShowFilter。**ダイアログ配置はWin32(GetWindowRect/MonitorFromWindow/GetMonitorInfo/SetWindowPos)で全てデバイスピクセルで行いオーナーのモニタ作業領域内にクランプ**=マルチDPI(混在スケール)でも必ずオーナーと同じモニタの右下に可視で出す(WPFのWindow.Left/Top+PointToScreen/TransformFromDevice方式はper-monitor DPIで座標がズレ画面外に出る不具合があった)。初期フィルター値はコンストラクタ(IsLoaded=false)で設定しLoaded中にメイン窓へコールバックしない(位置確定を乱さない)。`FilterDialog(initial,initialCount,apply)`) / O:相手ペインと同じパスへ移動 / Q:カーソル項目のフルパスをクリップボードへコピー / L:ドライブ / A:お気に入り登録 / 1:お気に入り移動 / H:フォルダー履歴(SelectionDialog・numbered・新しい順) / L:ドライブ(ドライブ文字キーで即移動) / 1:お気に入り移動(編集・削除可) / **T:組み込みターミナル**(`OpenOrFocusTerminal`。非アクティブペイン領域にTerminalPanelを表示=そちらのファイル一覧`LeftPaneContent`/`RightPaneContent`を一時非表示、`LeftTerminalHost`/`RightTerminalHost`(ContentControl)へ表示。**前回開いた種別**(`TerminalPreferenceStore`、無ければ検出先頭)・**カーソル位置のフォルダー**で起動(`TerminalCwd`=`TargetFolderPath`=カーソルがサブフォルダー上ならそのフォルダー/ファイル・".."上は現在ディレクトリ。書庫内は書庫のあるフォルダー)。表示中のTは既存ターミナルへフォーカス) / **Shift+T:種類を選んで新タブ**(SelectionDialog numbered→`OpenTerminalWithPicker`。選んだ種別が次回の既定として保存される) / **F1:通常⇄全画面トグル**(ファイル一覧・ターミナル・各プレビュー共通。隠す状態は廃止。端末入力中はterminal.htmlがF1横取り。端末のF1は犠牲) / ターミナル表示中のTab=一覧→ターミナルへフォーカス・ターミナル内Ctrl+T=一覧へフォーカス戻し・全タブ閉じるとパネルが畳まれ一覧復帰 / **V/G/B/Shift+K:外部ツール**(ユーザー定義リスト。既定=VSCode/Windows Terminal/Git Bash/SkimDown。設定Zで追加/削除/キー変更・引数マクロ・アプリ選択。`mem:features-external-tools`) / Z:設定 / F5:更新。フォルダーごとにカーソル位置を記憶し再訪時に再現(セッション中のみ)。↑↓/PageUp/Down/EndはOnPreviewKeyDownで明示処理。8/2/4/6(テンキーNumPad・上段数字Dとも)は↑/↓/←/→と同等(同caseにラベル追加)。↑↓は端でループ(MoveCursorWrap)、PageUp/Down/Endはクランプ。**Home=全選択⇔全選択解除トグル**(`PaneState.ToggleMarkAll`。対象はファイルのみ=フォルダーと".."は対象外。全ファイルがマーク済みなら全ファイル解除、でなければ全ファイルをマーク。フォルダーの個別マークは保持)。画像プレビュー中は↑↓に加えSpaceでも次画像へ。フッター(`KeyHelpText`)のキー操作説明はCtrl押下中=`CtrlKeyHelp`/Shift押下中=`ShiftKeyHelp`(Shift+Enter・Shift+K・Shift+T・Shift+G)/それ以外=`NormalKeyHelp`を表示(Ctrl優先)。`UpdateKeyHelp`を`OnPreviewKeyDown`冒頭・`OnPreviewKeyUp`(Left/RightのCtrl・Shift解放)・`Deactivated`で切替。

## テーマ(外観)
- 全ウィンドウ共通のセマンティックブラシを`App.xaml`のマージ辞書に持ち、各XAMLは`DynamicResource`(`App.Background`/`Fg.Primary`/`Accent`等のキー)で参照=ハードコード色なし。テーマ切替は辞書差し替えで即時反映。
- テーマ辞書`src/Filer.App/Themes/{Dark,Light,Beige,Green,Nord,Solarized,Dracula}.xaml`(同一キー集合・値だけ差替)。ダーク=既定/ライト=白基調/ベージュ=暖色アースカラー/グリーン=セージ・フォレスト/Nord=寒色ダーク/Solarized=暖色ライト/Dracula=紫系ダーク。
- `Core.AppTheme`{Dark,Light,Beige,Green,Nord,Solarized,Dracula,System}を`AppSettings.Theme`で保持→`settings.json`の`theme`へ永続化(未設定/未知値=Dark)。`AppSettingsStore`が読み書き(`AppSettingsStoreTests`にテーマ往復テスト)。
- `App.ThemeManager.Apply(AppTheme)`が`Application.Current.Resources.MergedDictionaries`のテーマ辞書を差替。`System`はレジストリ`HKCU\...\Themes\Personalize\AppsUseLightTheme`を読み起動時(`App.OnStartup`)と設定変更時(`ShowSettingsDialog`)にライト/ダークへ解決。
- **Markdownプレビューもテーマ追従**: `ThemeManager.CurrentMarkdownColors()`が現在のテーマ辞書ブラシから`Core.ThemeColors`(HTML/CSS用色文字列)を生成→`MarkdownRenderer.ToHtmlDocument(md,colors)`がCSSとmermaidテーマ(背景輝度でdark/default)を生成。`PreviewWindow.LoadMarkdownAsync`が描画時に渡す(開いている最中の切替には非追従)。`ToHtmlDocument(md)`単体版はDark既定。
- ファイル一覧の列ヘッダー(名前/拡張子/サイズ/更新日時)もMainWindowの`GridViewColumnHeader`暗黙スタイルでテーマ追従(`Panel.Background`/ホバー`Control.Background`/列区切り線。Headerは文字列+ソート▲▼なのでContentPresenterで表示)。
- `SelectionDialog`(ドライブL/お気に入り1/履歴H共用)のListBox項目、`AppPickerDialog`のListView項目・列ヘッダーもテーマ追従の項目スタイルを持つ(選択=`Selection.Background`/ホバー=`Row.Hover`。既定の青ハイライトを使わない)。
- **ウィンドウのタイトルバー(OSキャプション)もテーマ追従**: `WindowThemeHelper`がDWM属性(`DWMWA_CAPTION_COLOR`/`TEXT_COLOR`/`BORDER_COLOR`=Win11+、`USE_IMMERSIVE_DARK_MODE`)で背景=`Surface.Background`/文字=`Fg.Primary`/枠=`Border.Default`、ダーク判定=`App.Background`輝度。`App.OnStartup`で`Window.Loaded`のクラスハンドラを登録し全ウィンドウ(メイン+ダイアログ)へ適用、テーマ切替時は`ThemeManager.Apply`末尾の`WindowThemeHelper.ApplyToAll()`で開いている全ウィンドウへ再適用。WPF MessageBoxはネイティブのため非対象。
- **スクロールバーもテーマ追従**: `src/Filer.App/Styles/Controls.xaml`(App全体へマージ)の暗黙`ScrollBar`スタイル=矢印なし・細い(12px)つまみ式。トラック`App.Background`/つまみ`Border.Control`(ホバー`Fg.Subtle`/ドラッグ`Fg.Muted`)。`DynamicResource`参照で切替追従。注: Controls.xamlのSourceは"Themes/"を含まないため`ThemeManager`の差し替え対象検出に掛からない。
- **ダイアログの標準ボタンは共通スタイル`DialogButton`**(`Styles/Controls.xaml`のkeyed Style。角丸3・テーマ色(`Control.Background`/`Border.Control`、ホバー`Control.BackgroundHover`、無効`Control.BackgroundDisabled`+`Fg.Disabled`))。各ダイアログは`Window.Resources`に`<Style TargetType="Button" BasedOn="{StaticResource DialogButton}"/>`を置いて暗黙適用(IncrementalSearchDialog/InputDialog(ZIP圧縮名・改名・新規フォルダ等)/FavoriteEditDialogが使用)。ダイアログにボタンを追加する際はこのスタイルを使い、独自テンプレートを直書きしない(CLAUDE.md「UI(ダイアログ)」)。
- **設定ダイアログのテーマはラジオ選択で即プレビュー**: `SettingsDialog.Theme_Checked`→`ThemeManager.Apply(SelectedTheme)`をOK前に即実行(全ウィンドウへライブ反映)。OKで確定、キャンセル/×/Escは`OnClosed`で開いた時点の`_originalTheme`へ戻す。初期化中は`_suppressThemeApply`でプレビュー抑止。
- 補足: 組み込みターミナルはWebView2上のHTMLで別系統テーマ=現状WPFテーマに非追従。

## ビルド/配布
- `dotnet build Filer.sln` / `dotnet test tests/Filer.Core.Tests/Filer.Core.Tests.csproj` / `dotnet watch run`
- 配布: `dotnet publish src/Filer.App/Filer.App.csproj -c Release -r win-x64 --self-contained -p:PublishSingleFile=true -o publish` → `publish/Filer.App.exe`
- 注意: 発行前に実行中の`Filer.App`プロセスを停止(単一exeロックでGenerateBundle失敗)。

## 注意 / 未実装
- serenaの対象言語は `csharp`(.serena/project.yml に明示設定済み。以前の python 誤検出は修正済み)。
- Markdown/HTMLプレビュー: WebView2ランタイム(Win11標準同梱)が必要。mermaid.jsは同梱でオフライン動作。大きな図は等倍表示＋スクロール、図ごとのツールバーで拡大/縮小/全画面。HTML/SVG等(Html種別)は`PreviewWindow.LoadHtmlAsync`が実ファイルの所在フォルダーを仮想ホストにマップして描画(相対参照CSS/画像/JSも解決)。Markdown/HtmlはSキーでレンダリング⇄ソース表示を切替(ヘッダーに[ソース]表示)。PDFと同様WebViewへフォーカスを移さず、↑↓/PgUp/PgDn/Home/EndはDevTools経由(`ScrollPdf`)でスクロール、Esc/Enterで終了。
- ZIP書庫: ナビゲーション+読み取り操作(コピー抽出/画像・テキスト・Markdown・HTMLプレビュー)対応。書庫内HTMLは単体ファイルとして作業フォルダーへ展開して表示(相対参照CSS/画像は再現しない)。**新規ZIP圧縮(Xキー、`ZipArchiver`)対応**。既存書庫の書き換え系とnested zipは非対応(明示メッセージで拒否)。
- タブ: 各ペインで複数タブの追加(Ctrl+T/＋)/削除(Ctrl+W/×。最後の1枚は残す)/切替(Ctrl+←/→/クリック)。タブ構成(全タブのパス+アクティブ位置)はセッション保存で永続化し次回起動時に復元(実在フォルダーのみ)。
- フォルダー履歴: フォルダー移動のたびに`HistoryStore`へ記録(最大50件・MRU・永続化)。Hキーで一覧表示しアクティブ側を移動。
- 設定(Z): タブ=キー割り当て/外部ツール/外観/**確認**。`%APPDATA%\Filer\settings.json`(キーは既定と異なる操作のみ差分保存)。設定UIはキャプチャ方式(押したキーをそのまま割り当て)。
- 確認ダイアログ設定(`AppSettings.ConfirmMove`/`ConfirmRecycle`(D)/`ConfirmPermanentDelete`(Shift+D)、いずれも既定true): MainWindowの各ハンドラが`ConfirmOrRun(needConfirm, msg, action)`で分岐。falseなら確認なしで即実行。
- 削除: **D/Delete=ごみ箱送り(`FileOperations.DeleteToRecycleBin`→`RecycleBin.Send`でSHFileOperation FO_DELETE+FOF_ALLOWUNDO)**、Shift+D=完全削除(`FileOperations.Delete`)。VM側は`DeleteTargets`(ごみ箱)/`DeleteTargetsPermanently`(完全)。画像プレビュー(PreviewWindow)のD/Deleteもごみ箱送り。
- 未実装: 書庫書き換え。
